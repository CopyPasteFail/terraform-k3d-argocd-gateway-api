apiVersion: v1
kind: Service
metadata:
  name: landing
  namespace: landing
  labels:
    app.kubernetes.io/name: landing
spec:
  selector:
    app.kubernetes.io/name: landing
  ports:
  - name: http
    port: 80
    targetPort: http
